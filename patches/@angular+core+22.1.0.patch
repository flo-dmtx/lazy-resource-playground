diff --git a/node_modules/@angular/core/fesm2022/_resource-chunk.mjs b/node_modules/@angular/core/fesm2022/_resource-chunk.mjs
index 41db17e..dfedd5d 100755
--- a/node_modules/@angular/core/fesm2022/_resource-chunk.mjs
+++ b/node_modules/@angular/core/fesm2022/_resource-chunk.mjs
@@ -5,7 +5,7 @@
  */
 
 import { inject, RuntimeError, formatRuntimeError, ErrorHandler, DestroyRef, InjectionToken, signalAsReadonlyFn, assertInInjectionContext, TransferState, effect, PendingTasks, signal, isSignal, Injector } from './_pending_tasks-chunk.mjs';
-import { setActiveConsumer, createComputed, SIGNAL } from './_effect-chunk.mjs';
+import { setActiveConsumer, createComputed, SIGNAL, consumerAfterComputation, consumerBeforeComputation, consumerDestroy, producerAccessed, producerUpdateValueVersion, REACTIVE_NODE, runPostProducerCreatedFn } from './_effect-chunk.mjs';
 import { untracked as untracked$1, createLinkedSignal, linkedSignalSetFn, linkedSignalUpdateFn } from './_untracked-chunk.mjs';
 
 class OutputEmitterRef {
@@ -155,7 +155,7 @@ function resource(options) {
   }
   const oldNameForParams = options.request;
   const params = options.params ?? oldNameForParams ?? (() => null);
-  return new ResourceImpl(params, getLoader(options), options.defaultValue, options.equal ? wrapEqualityFn(options.equal) : undefined, options.debugName, options.injector ?? inject(Injector), options.id);
+  return new ResourceImpl(params, getLoader(options), options.defaultValue, options.equal ? wrapEqualityFn(options.equal) : undefined, options.debugName, options.injector ?? inject(Injector), options.id, undefined, options.lazy ? 'lazy' : 'eager');
 }
 class BaseWritableResource {
   value;
@@ -201,6 +201,31 @@ class BaseWritableResource {
     return this;
   }
 }
+const UNSET_PULL = /* @__PURE__ */Symbol('UNSET_PULL');
+const RESOURCE_PULL_NODE = /* @__PURE__ */(() => {
+  return {
+    ...REACTIVE_NODE,
+    value: UNSET_PULL,
+    dirty: true,
+    producerMustRecompute: node => node.value === UNSET_PULL,
+    producerRecomputeValue: node => {
+      const prevConsumer = consumerBeforeComputation(node);
+      try {
+        const extRequest = node.readExtRequest();
+        node.value = extRequest;
+        node.version++;
+        const consumer = setActiveConsumer(null);
+        try {
+          node.load();
+        } finally {
+          setActiveConsumer(consumer);
+        }
+      } finally {
+        consumerAfterComputation(node, prevConsumer);
+      }
+    }
+  };
+})();
 class ResourceImpl extends BaseWritableResource {
   loaderFn;
   equal;
@@ -210,6 +235,7 @@ class ResourceImpl extends BaseWritableResource {
   state;
   extRequest;
   effectRef;
+  pullNode;
   pendingController;
   resolvePendingTask = undefined;
   destroyed = false;
@@ -217,11 +243,12 @@ class ResourceImpl extends BaseWritableResource {
   status;
   error;
   transferState;
-  constructor(request, loaderFn, defaultValue, equal, debugName, injector, transferCacheKey, getInitialStream) {
+  constructor(request, loaderFn, defaultValue, equal, debugName, injector, transferCacheKey, getInitialStream, loadStrategy = 'eager') {
     if (isInParamsFunction()) {
       throw invalidResourceCreationInParams();
     }
     super(computed(() => {
+      this.pull();
       const streamValue = this.state().stream?.();
       if (!streamValue) {
         return defaultValue;
@@ -323,15 +350,37 @@ class ResourceImpl extends BaseWritableResource {
       },
       ...(ngDevMode ? createDebugNameObject(debugName, 'state') : undefined)
     });
-    this.effectRef = effect(this.loadEffect.bind(this), {
-      injector,
-      manualCleanup: true,
-      ...(ngDevMode ? createDebugNameObject(debugName, 'loadEffect') : undefined)
-    });
+    if (loadStrategy === 'lazy') {
+      const node = Object.create(RESOURCE_PULL_NODE);
+      node.readExtRequest = () => this.extRequest();
+      node.load = () => {
+        if (!this.destroyed) {
+          this.loadEffect();
+        }
+      };
+      if (typeof ngDevMode !== 'undefined' && ngDevMode) {
+        node.debugName = createDebugNameObject(debugName, 'pull')?.debugName;
+      }
+      runPostProducerCreatedFn(node);
+      this.pullNode = node;
+    } else {
+      this.effectRef = effect(this.loadEffect.bind(this), {
+        injector,
+        manualCleanup: true,
+        ...(ngDevMode ? createDebugNameObject(debugName, 'loadEffect') : undefined)
+      });
+    }
     this.pendingTasks = injector.get(PendingTasks);
     this.unregisterOnDestroy = injector.get(DestroyRef).onDestroy(() => this.destroy());
-    this.status = computed(() => projectStatusOfState(this.state()), ngDevMode ? createDebugNameObject(debugName, 'status') : undefined);
+    this.status = computed(() => {
+      if (this.destroyed) {
+        return 'idle';
+      }
+      this.pull();
+      return projectStatusOfState(this.state());
+    }, ngDevMode ? createDebugNameObject(debugName, 'status') : undefined);
     this.error = computed(() => {
+      this.pull();
       const stream = this.state().stream?.();
       return stream && !isResolved(stream) ? stream.error : undefined;
     }, ngDevMode ? createDebugNameObject(debugName, 'error') : undefined);
@@ -340,11 +389,12 @@ class ResourceImpl extends BaseWritableResource {
     if (this.destroyed) {
       return;
     }
-    const error = untracked(this.error);
     const state = untracked(this.state);
-    if (!error) {
+    const streamValue = state.stream && untracked(state.stream);
+    const error = streamValue && !isResolved(streamValue) ? streamValue.error : undefined;
+    if (!error && state.status === 'local') {
       const current = untracked(this.value);
-      if (state.status === 'local' && (this.equal ? this.equal(current, value) : current === value)) {
+      if (this.equal ? this.equal(current, value) : current === value) {
         return;
       }
     }
@@ -377,7 +427,10 @@ class ResourceImpl extends BaseWritableResource {
   destroy() {
     this.destroyed = true;
     this.unregisterOnDestroy();
-    this.effectRef.destroy();
+    this.effectRef?.destroy();
+    if (this.pullNode !== undefined) {
+      consumerDestroy(this.pullNode);
+    }
     this.abortInProgressLoad();
     this.state.set({
       extRequest: {
@@ -389,6 +442,13 @@ class ResourceImpl extends BaseWritableResource {
       stream: undefined
     });
   }
+  pull() {
+    if (this.pullNode === undefined || this.destroyed) {
+      return;
+    }
+    producerUpdateValueVersion(this.pullNode);
+    producerAccessed(this.pullNode);
+  }
   async loadEffect() {
     const extRequest = this.extRequest();
     const {
diff --git a/node_modules/@angular/core/types/_api-chunk.d.ts b/node_modules/@angular/core/types/_api-chunk.d.ts
index a41a786..044728b 100755
--- a/node_modules/@angular/core/types/_api-chunk.d.ts
+++ b/node_modules/@angular/core/types/_api-chunk.d.ts
@@ -285,6 +285,15 @@ interface BaseResourceOptions<T, R> {
      * This value value needs to be identical for both the client and server.
      */
     id?: string;
+    /**
+     * Whether the resource defers loading until the first time one of its signals is read.
+     *
+     * A lazy resource performs no work at creation. `params` changes while unread do not trigger
+     * loads; the first read uses the latest params. Not recommended with `id`.
+     *
+     * Defaults to `false` (the loader runs eagerly whenever `params` change).
+     */
+    lazy?: boolean;
 }
 /**
  * Options to the `resource` function, for creating a resource.
